apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: repoconfigurations.gitlab.ogen.krateo.io
spec:
  conversion:
    strategy: None
  group: gitlab.ogen.krateo.io
  names:
    categories:
    - repo
    - restconfigs
    - rc
    kind: RepoConfiguration
    listKind: RepoConfigurationList
    plural: repoconfigurations
    singular: repoconfiguration
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          spec:
            properties:
              authentication:
                description: The authentication methods available for this API.
                properties:
                  bearer:
                    properties:
                      tokenRef:
                        properties:
                          key:
                            type: string
                          name:
                            type: string
                          namespace:
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                    required:
                    - tokenRef
                    type: object
                type: object
              configuration:
                properties:
                  query:
                    properties:
                      findby:
                        properties:
                          membership:
                            type: boolean
                          per_page:
                            default: 100
                            type: integer
                          simple:
                            type: boolean
                        type: object
                    type: object
                type: object
            type: object
        type: object
    served: true
    storage: true
